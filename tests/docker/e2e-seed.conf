[Service]
ExecStartPre=-/bin/sh -c 'install -d -m 0750 /etc/rayfish; [ -e /etc/rayfish/settings.toml ] || { printf "mdns_enabled = false\\n" > /etc/rayfish/settings.toml; chmod 600 /etc/rayfish/settings.toml; }'
